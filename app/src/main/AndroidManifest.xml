<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Local notifications for new agent replies (AgentReplyPollWorker). On
         API 33+ this is a runtime permission requested in MainActivity; on
         API 26-32 it is granted at install time. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- REQUEST_INSTALL_PACKAGES lives in the `sideload` flavor only
         (app/src/sideload/AndroidManifest.xml) — the Play build must not have it. -->

    <application
        android:name=".HumanosApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Humanos">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Humanos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging service. Fires onMessageReceived in the
             foreground; backgrounded/killed pushes are shown by the system tray
             via the message's `notification` block (channel "agent_messages"). -->
        <service
            android:name="eco.humanos.android.core.notifications.HumanosMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Grants the system installer temporary read access to the downloaded
             APK (see ApkInstaller). Authority matches "${applicationId}.fileprovider". -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
