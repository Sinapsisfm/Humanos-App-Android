package eco.humanos.android.core.network

/**
 * Network module — Retrofit service interfaces and OkHttp configuration
 * will be added in Tanda 5.
 */
object NetworkConstants {
    // Base URLs will come from BuildConfig / remote config
    const val CONNECT_TIMEOUT_SECONDS = 30L
    const val READ_TIMEOUT_SECONDS = 30L
    const val WRITE_TIMEOUT_SECONDS = 30L
}
